pipeline {
  agent {
    kubernetes {
      label 'mangy-eel-jenkins-slave'
      defaultContainer 'docker'
  
}
  }
  stages {
    

    stage('Deploy Canary') {

      steps {
        container('docker') {
          // Change deployed image in canary to the one we just built
  //        sh("docker build -t myimage:v1 .")
          
   
        } 
      }
    }
   
   
  }
}
